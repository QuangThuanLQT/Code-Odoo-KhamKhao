<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="vieterp_tour_manager.list">
      <field name="name">vieterp_tour_manager list</field>
      <field name="model">vieterp_tour_manager.vieterp_tour_manager</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="vieterp_tour_manager.action_window">
      <field name="name">vieterp_tour_manager window</field>
      <field name="res_model">vieterp_tour_manager.vieterp_tour_manager</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="vieterp_tour_manager.action_server">
      <field name="name">vieterp_tour_manager server</field>
      <field name="model_id" ref="model_vieterp_tour_manager_vieterp_tour_manager"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="vieterp_tour_manager" id="vieterp_tour_manager.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="vieterp_tour_manager.menu_1" parent="vieterp_tour_manager.menu_root"/>
    <menuitem name="Menu 2" id="vieterp_tour_manager.menu_2" parent="vieterp_tour_manager.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="vieterp_tour_manager.menu_1_list" parent="vieterp_tour_manager.menu_1"
              action="vieterp_tour_manager.action_window"/>
    <menuitem name="Server to list" id="vieterp_tour_manager" parent="vieterp_tour_manager.menu_2"
              action="vieterp_tour_manager.action_server"/>
    -->
  </data>
</odoo>